<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <Button
        android:id="@+id/slidein_button"
        android:layout_width="192dp"
        android:layout_height="48dp"
        android:text="SIMPLE"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/circular_progress_button"
        app:layout_constraintVertical_chainStyle="packed"
        android:layout_marginBottom="24dp"
        android:onClick="goToSlideInActivity"/>

    <Button
        android:id="@+id/circular_progress_button"
        android:layout_width="192dp"
        android:layout_height="48dp"
        android:text="CIRCULAR PROGRESS"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/slidein_button"
        app:layout_constraintBottom_toBottomOf="parent"
        android:onClick="goToCircularProgressBarActivity"/>

</androidx.constraintlayout.widget.ConstraintLayout>
